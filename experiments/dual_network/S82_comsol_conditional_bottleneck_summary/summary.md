# S82 COMSOL conditional bottleneck decision summary

## 1. ??????

S74 ???????? COMSOL geometry-variation multi-height Bz ???converted NPZ ?? conditional runner ???

- train signals shape: `[50,3,200]`
- val signals shape: `[10,3,200]`
- test signals shape: `[10,3,200]`
- flattened encoder length: `600`

S75 ?????????? train / val / test conditional supervised probe?held-out IoU ? `0.40`??????? synthetic single-Bz conditional ??? `0.1` ? held-out IoU?

## 2. target / mask ??

S77 ?? `mu_maps < 500` ? provided `masks > 0.5` ? train / val / test ??????avg mask IoU = `1.0`?mismatch count = `0`?target/mask ?????????????

## 3. mask_source ??

S78 ?? `mask_source=masks` ???? `mask_source=mu_threshold`????? label ??????????? `mask_source=mu_threshold`?

## 4. train-fit ??

S79 ??? steps??? point subsample??? BCE ?????? train IoU ??? `0.70`???????? held-out IoU??? train fit ??????????????????? BCE ?????????

## 5. output head ??

S81 ??? `mu_threshold_reference`?`direct_mu0` ? `direct_mu1e-5`?

- `direct_mu0` ?????? train/val/test IoU ???????? S75/S78 baseline?
- `direct_mu1e-5` ???? `mu_mse` / `mu_mae`???????? mask IoU?
- direct head ?????????????`mu_threshold` baseline ?????

## 6. ??????

1. `data size/diversity`: ???? train 50 / val 10 / test 10?? `defect_type=ellipsoid` ????? rotation ? boundary irregularity?
2. `model/conditioning`: ?? conditional MLP/latent conditioning ??????????? Bz ???????? geometry?
3. `loss/output head`: direct head ??? `mu_mse` ????????????? held-out IoU?
4. `train fitting`: train IoU ??????????? steps/subsample/BCE ?????
5. `target/mask`: S77/S78 ???????????

## 7. ?????

- ?? `mu_threshold` conditional baseline?
- ????????????? COMSOL geometry-variation ???????????
- V2 ????? `defect_type`?rotation angle ? boundary irregularity??????? `defect_params` metadata?
- ?? V2 ? val/test ???????? model/conditioning ??????? loss????????????
