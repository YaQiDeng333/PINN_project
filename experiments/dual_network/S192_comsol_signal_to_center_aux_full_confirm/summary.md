# S192 COMSOL signal-to-center auxiliary full confirm summary

S192 was skipped.

Reason: S191 did not identify an auxiliary configuration that met the gate against the same-round `current_candidate_reference`. Both auxiliary variants had lower val/test IoU and worse held-out `center_grid_mae` than the reference.

No 3000-step auxiliary full confirm was run. The current COMSOL parametric candidate remains S185 `center_bin_offset_plus_grid`.
