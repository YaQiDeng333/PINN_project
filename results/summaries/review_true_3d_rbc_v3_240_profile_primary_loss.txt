必须修复：
无。上一轮 review 指出的两个 must-fix 已关闭：profile-primary candidate selection 使用 20.77 validation Dice `ref_val["dice"]=0.831555`，不是 20.77 test Dice；`train_true_3d_rbc_profile_primary_model.py --variant` 在 Stage C gate failed 时默认拒绝绕过，只有显式 `--force-after-failed-gate` 才能做诊断 rerun。

建议修复：
无阻塞项。后续若复用 20.81 comparator 常量，建议在 summary 中继续写明它只是 reference/comparator，不调用 20.81 feature-fusion 脚本做新的选择。

可以接受：
可以接受为一次失败候选筛查。显式通过 COMSOL_DATA_REGISTRY.md + manifest + dataset_id 加载 v3_240；未使用 latest/newest NPZ scan；未运行 COMSOL；未生成或修改数据/NPZ；未创建 baseline；未修改 CURRENT_BASELINE 或 COMSOL baseline docs。模型输入仍是 delta_b -> 9x201；rbc_params/profile_depth_grid/projected_mask 只作为 loss/metrics/supervision；torch profile generator 与 dataset NumPy generator consistency 全 pass；eligible_for_multiseed=False 且未跑 multi-seed；gallery 只用于 held-out test 可视化，包含 best/worst/risk 样本，不参与 selection。

是否认为 profile-primary loss 有实质价值：
不认为有主线升级价值。20.83 validation-selected `P2_profile_primary_dim_guard_w1p0` 把 projected mask Dice 提到 0.868042，但 profile_depth_rmse_m=0.000409718，差于 20.77 neural reference 的 0.000387737。

是否同意 wMAE 只作辅助诊断：
同意。20.83 的 pass/fail 分界点是 comparable profile RMSE / profile-level reconstruction；wLD/wWD/wLW MAE 继续报告，但不决定 gate 通过。

下一步更建议什么：
B. keep 20.77/20.81 candidate。保留 20.77 作为 profile reference，保留 20.81 作为 visual/mask comparator；不跑 Stage C multi-seed，不更新 baseline，把 20.83 记为 negative experiment。

简短原因：
R1 profile-primary loss 没赢主指标，只有 2D mask/Dice 改善。目标已经从 wMAE 转向 3D profile，而本轮没有提升 3D profile reconstruction，因此不能升级 candidate。
