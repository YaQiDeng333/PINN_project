22.1 shape-conditioned / two-stage internal defect model 独立只读 review

review_pass: true
must_fix: 无

核对范围：
- 工作目录和 git root 已确认在 `C:\Users\19166\Desktop\PINN_project`。
- 未发现 COMSOL 运行、data/NPZ 生成或修改、`CURRENT_BASELINE.md` 修改。
- `dataset_id=comsol_internal_defect_pilot_pack_v2_240`，通过 `load_dataset(args.dataset_id)` 走 registry/manifest gate；未发现 latest/newest scan。
- 正式输入来自 `dataset.x_channels` 即 `delta_b` reshape 后的 Bx/By/Bz 9 通道，以及 `extract_features(dataset.delta_b)` 生成的 delta_b-derived features。
- true `shape_type` 只用于 supervision / metrics；`T4_oracle_true_shape_diagnostic` 明确标记为 `oracle_diagnostic`，且 `valid_for_multiseed=false`，没有被作为正式候选或 selected candidate。
- candidate screen 和 multi-seed seed selection 均为 validation-only；T3 multi-seed 选中 seed `123`，该 seed 的 test 不是最优，说明没有 test 反选。

关键指标：
- selected_candidate = `T3_shape_specific_heads`
- selected_seed = `123`
- test total normalized MAE = `0.357371`
- test center p95/max = `5.999 / 10.468 mm`
- test burial p95/max = `1.690 / 1.848 mm`
- test catastrophic failure count = `5`
- test geometry_branch_failure count = `1`
- test shape accuracy/F1 = `0.950000 / 0.950549`

结论：
- T3 改善了 total normalized MAE 和 center p95，但没有降低 catastrophic failure count，也没有降低 geometry_branch_failure count。
- route decision 正确写为 `stable_inference_candidate=false`，internal branch 仍不是 `CURRENT_BASELINE`。
- 允许进入 Markdown 同步和 commit；提交时继续排除 data/、NPZ、checkpoints/、preview PNG、notes、`CURRENT_BASELINE.md` 和 `scripts/visualize_current_baseline.py`。
