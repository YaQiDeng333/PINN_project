20.96 liftoff-conditioned inference smoke review

必须修复：无。
建议修复：无；可在文档中说明 baseline manifest 是 20.88a recovery artifact，但复现 20.77/20.85 baseline，避免 stage 命名误读。
可以接受：可以接受。
是否认为 inference runner 可用于下一步 real-data schema intake：是。

原因：
- 0.012m 边界误标已修复；contract check 显示 0.006/0.012 合法，0.005999/0.012001/0.02 越界。
- 数据读取固定 dataset_id=comsol_true_3d_rbc_liftoff_aug_pack_v1，并通过 registry + manifest gate，无 latest/newest scan。
- runner 只加载 20.85 baseline artifact 和 20.94/20.96a A2 artifact，不训练、不运行 COMSOL、不写 data/NPZ/checkpoint。
- routing 符合 nominal baseline / non-nominal baseline_plus_adapter；缺失 sensor_z_m 报错，越界 sensor_z_m 标记 out_of_range。
- smoke metrics 覆盖 auto / force_baseline / force_adapter，metadata contract 完整，CURRENT_BASELINE 未改，A2 被定位为 companion module。
