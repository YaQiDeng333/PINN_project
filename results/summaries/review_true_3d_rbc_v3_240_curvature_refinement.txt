必须修复：
- 无。

建议修复：
- 建议在后续正式汇总中强调 C2 的 test 改善不能作为选择依据，因为 candidate screen 明确用 validation-only selection，最终 selected candidate 是 C1_split_heads。
- 建议下一步不要升级 20.79 refined model；保留 20.77 benchmark candidate，并优先做 exact Piao/NLS-inspired feature diagnostic 或 curvature-targeted data。

可以接受：
- 可以接受。检查显示 20.79 通过 COMSOL_DATA_REGISTRY.md + manifest 显式 dataset_id 加载 v3_240，loader 明确禁止 latest/newest NPZ fallback。
- 未发现 COMSOL 运行、新数据生成、NPZ 修改、checkpoint/preview 输出或 baseline 文档修改证据。
- 模型输入边界正确：训练脚本使用 delta_b 归一化输入；C3 feature fusion 只从 delta_b 派生并用 train split scaler；rbc_params/projected_mask/split/template/depth_bin/aspect_bin 用于 supervision 或 metrics/grouping，不作为模型输入。
- train-only normalization、validation-only candidate/seed selection、test-final-only reporting 的边界在脚本和 summary 中一致。
- selected candidate multi-seed 已按 C1_split_heads + seeds 42/123/2026 执行；seed 42 由 validation score 选中。
- metrics 和 summaries 一致：selected refined test normalized MAE=0.753387，curvature MAE=0.211584，L/W/D=2.660/2.135/1.112 mm，Dice=0.834597；相对 20.77 reference 均未形成可靠提升。
- route decision 合理：E_severe_regression_revert_to_20_77_candidate。

是否认为 curvature-aware refinement 有实质价值：
- 当前 20.79 selected refined model 没有实质模型升级价值。它作为诊断有价值：split-head/refinement 方向在 validation-selected 口径下没有修复 curvature，且牺牲了 L_m、D_m、total MAE 和 mask Dice。

是否建议 formal benchmark rerun / exact Piao features / targeted data：
- 不建议用 20.79 refined model 做 formal benchmark rerun 或 candidate upgrade。
- 建议保留 20.77 v3_240 benchmark candidate；下一步优先 exact Piao/NLS-inspired feature pipeline，其次 curvature-targeted data top-up。

简短原因：
- 20.79 遵守数据治理和训练边界，但 selected validation model C1_split_heads 在 test 上 curvature 从 0.201076 退化到 0.211584，total normalized MAE 从 0.678014 退化到 0.753387，L_m/D_m 和 projected mask Dice 也退化。因此 refinement 结论应是 revert to 20.77 benchmark candidate，而不是升级模型。
