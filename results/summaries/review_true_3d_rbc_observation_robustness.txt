20.88 true 3D RBC observation perturbation robustness review

Review mode:
- Independent read-only review agent.
- No file modifications performed by review agent.

Must-fix:
- The stale preflight-blocker review text in this file contradicted the current 20.88 run. The main controller replaced it with this current review record before handoff.

Suggestions handled by main controller:
- The robustness script now rejects any artifact manifest path other than results/manifests/true_3d_rbc_baseline_inference_artifact_manifest.json.
- Clean baseline values used for degradation calculations now come from the 20.88a artifact manifest test_metrics, not separate hard-coded constants.

Acceptable:
- Dataset loading uses COMSOL_DATA_REGISTRY.md + manifest for dataset_id=comsol_true_3d_rbc_imported_watertight_pilot_v3_240.
- No latest/newest NPZ scan is used.
- The audit reuses the 20.88a artifact manifest and ignored checkpoint.
- No COMSOL run was performed.
- No training was performed.
- No data or NPZ file was generated or modified.
- Perturbations are applied only to in-memory delta_b/x_channels.
- Model input is only perturbed delta_b/BxByBz; rbc_params, projected_mask, profile_depth, split, curvature_template, depth_bin, aspect_bin, and sample_id are used only for metrics/group/failure-case reporting.
- The requested perturbation groups are covered: additive noise, global gain, per-axis gain, zero drift, no-defect reference error, sensor_x jitter, channel dropout/attenuation, combined_light, and combined_hard.
- Metrics and decision summaries are consistent: test status counts are green=17, warning=3, fail=11.
- wMAE is treated as auxiliary diagnostic only.

Can accept:
- Yes.

Route decision:
- The audit is not an overall robustness pass. Noise <=10%, no-defect reference error, and sensor_x jitter are relatively stable, but gain scaling, combined perturbations, and Bx channel attenuation/dropout are sensitive.
- Next recommended action is gain/amplitude calibration or augmentation planning, while keeping 20.89 liftoff/sensor-offset COMSOL diagnostic pack as the next physics-side robustness diagnostic.

Reason:
- noise_10pct remains green with profile degradation 4.095415% and no Dice loss, but gain_scaling_0.8x causes 123.845240% profile RMSE degradation, channel_attenuation_Bx_50pct causes 141.577253% degradation, and channel_dropout_Bx_missing causes 0.163825 Dice drop. This supports a bounded robustness conclusion rather than a broad robust claim.
