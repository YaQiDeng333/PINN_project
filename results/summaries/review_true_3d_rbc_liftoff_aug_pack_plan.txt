Review: 20.91 true 3D RBC liftoff augmentation pack plan

Scope reviewed:
- Plan-only stage for dedicated liftoff COMSOL augmentation pack.
- No COMSOL execution, no data/NPZ generation, no training, no checkpoint or preview generation.
- No CURRENT_BASELINE.md update.

Must-fix:
- None.

Suggestions:
- Add exact COMSOL_DATA_REGISTRY.md path and resolved v3_240 manifest path into the preflight and plan summaries so the human-facing artifacts show the same explicit dataset gate as the script.

Action taken:
- Suggestion adopted. The plan script now writes both registry_path and manifest_path into the preflight summary and plan summary.
- The plan script was rerun with --overwrite after this update.

Acceptable:
- Yes.

Reviewer conclusion:
- The plan is acceptable as a plan-only 20.91 stage.
- The plan CSV contains 48 base geometries and 192 planned rows.
- Every selected base has paired liftoff levels sensor_z_m = 0.006, 0.008, 0.010, 0.012.
- CURRENT_BASELINE.md is not modified.
- No data, NPZ, .mph, raw CSV, checkpoint, preview PNG, notes, or temp STL artifacts are staged.
- COMSOL dirty files are pre-existing and unrelated; they should not be staged or committed for this plan-only PINN commit.

Route recommendation:
- Proceed to the dedicated liftoff COMSOL pack generation only as a separate confirmed execution stage.
- Keep calibration as a diagnostic caveat.
- Keep internal/buried defect feasibility deferred.
- Plan 20.92 as an ablation between the current unconditioned model family and a scalar sensor_z_m conditioned liftoff-aware model.
