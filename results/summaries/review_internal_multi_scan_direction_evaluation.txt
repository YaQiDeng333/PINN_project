23.3 internal multi-scan-direction diagnostic evaluation review

review_scope: read-only independent review agent
review_result: passed_after_should_fix

must_fix:
- 无。

should_fix:
- route decision 初版把 cuboid/ellipsoid confusion 与 center/burial tail 两个中间判断写成 yes，但证据是混合信号。已修复为 mixed，并重跑 `scripts/decide_internal_multi_scan_direction_evaluation_route.py`。

passed_checks:
- 新增脚本显式使用 dataset_id = comsol_internal_defect_multi_scan_direction_pack_v1，并通过 COMSOL_DATA_REGISTRY.md 与 `results/manifests/comsol_internal_defect_multi_scan_direction_pack_v1.manifest.json` 加载；未使用 latest/newest scan。
- diagnostic probe 输入只来自 delta_b / BxByBz 派生特征；shape_type、burial_depth_level、size_level、aspect_bin、split、sample_id 仅用于 supervision、metrics 或 group audit。
- 本轮是 lightweight diagnostic evaluation；没有写 checkpoint，没有保存模型，没有把结果写成 formal model candidate 或 baseline。
- 未运行 COMSOL，未训练 formal model，未修改 data/NPZ，未更新 CURRENT_BASELINE.md。
- 输出覆盖 preflight、paired audit、feature separability、diagnostic probe、route decision。
- route decision 合理：dual-direction 没有清晰优于 x-only，23.3 不进入 23.4 training gate。

review_conclusion:
23.3 可以作为 internal multi-scan-direction diagnostic evaluation 提交。dual-direction 提供非冗余观测，但当前 probe 证据不足以支持进入训练 gate；下一步应继续做更高信息量观测诊断，而不是把 dual-direction 写成稳定模型路线。
