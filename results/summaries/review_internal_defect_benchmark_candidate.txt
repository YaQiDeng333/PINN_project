21.7 internal defect benchmark candidate read-only review

必须修复：无。

建议修复：
- `internal_defect_benchmark_rerun_b2_metrics.csv` 里用于标记选择的列名原本是 `selected_candidate`，不是 `selected`；这不是结果错误，但后续人工查表时容易误读。主控已保留 `selected_candidate` 并补充 `selected` 兼容列，summary 仍正确记录 selected seed=2026。

可以接受：
- 可以接受。
- dataset 通过 `COMSOL_DATA_REGISTRY.md` 和 `results/manifests/comsol_internal_defect_pilot_pack_v2_240.manifest.json` 显式加载。
- 未发现 latest/newest scan。
- 未发现 COMSOL、data/NPZ、`CURRENT_BASELINE.md` 或 baseline 文档改动。
- B2 输入边界符合要求，只用 `delta_b/BxByBz` 和 delta_b-derived features。
- selection protocol 是 train-only normalization、validation-only selection、test final only。

是否认为 B2 有实质 benchmark candidate 价值：
- 是。B2 selected seed=2026，test total MAE=0.395256，burial_depth MAE=0.413 mm，shape accuracy/F1=0.975000/0.975309；三 seed burial MAE 都优于 21.4 neural 和 feature baseline，说明不是单 seed 偶然。

是否同意不能称 baseline：
- 同意。internal defect 仍是独立分支，`CURRENT_BASELINE.md` 仍应保持 surface/near-surface true 3D RBC baseline；B2 目前只能称 internal benchmark candidate。

下一步建议：
- 进入 internal report / visualization package，先把 B2 的 by-shape、by-burial、failure cases 和可视化整理出来，再考虑真实 internal sample schema 或更大 shape 扩展。

简短原因：
- 21.7 已经证明 B2 在 burial_depth 和 total score 上稳定超过两个 reference，但数据仍是 COMSOL 仿真，shape 只覆盖 sphere/ellipsoid/cuboid，尚未具备 baseline transition 条件。
