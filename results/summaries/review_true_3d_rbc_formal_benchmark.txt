- 必须修复：无。

- 建议修复：
  1. 提交前显式检查 `git diff --cached --name-only`，因为 `results/` 被 ignore，后续若要提交 metrics/summary 需要白名单 `git add -f results/...`，但不能误加 preview/data。
  2. comparison 中 Er-like profile error 对历史 20.77/20.81 artifacts 不是完整同口径，当前主判断应继续以 `profile_depth_rmse_m` 为核心证据。

- 可以接受：可以接受。检查到 20.85 通过 registry/manifest 显式加载 `comsol_true_3d_rbc_imported_watertight_pilot_v3_240`，无 latest/newest scan；训练脚本复用 20.77 `RBCConvRegressor`、weighted SmoothL1、train-only normalization、validation-only checkpoint/seed selection，test 只用于最终汇总；输入链路是 `delta_b -> x_channels (9,201)`，metadata/labels 只用于 supervision/metrics/grouping。

- 是否认为 formal rerun 可作为 true 3D RBC benchmark candidate：同意。formal rerun 选中 seed=42，稳定复现 20.77：test normalized MAE `0.678014`，profile RMSE `0.000387737 m`，Dice `0.847727`；profile RMSE 优于 20.81 `0.000445297` 和 20.83 `0.000409718`。

- 是否同意仍不能称 baseline：同意。summary/decision 明确 `baseline_ready=false`、不更新 `CURRENT_BASELINE.md`，这仍是 fixed-dataset benchmark candidate，不是 baseline replacement。

- 下一步更建议什么：基于 20.77/formal rerun profile-depth candidate 准备论文/报告展示；20.81 作为 projected-mask/visual comparator，20.83 作为 profile-primary negative evidence，不继续小修 20.83。

- 简短原因：20.85 的关键优势是 profile/depth 主指标稳定复现，而不是 Dice 最高；数据治理、输入边界、selection protocol 和 baseline 边界都符合本轮规则。
