21.2 internal defect training gate 独立只读 review

必须修复：
- 无。

建议修复：
- 已处理：`neural beat feature baseline` 改成更精确口径。neural 在 regression + shape accuracy 的综合 selection score 上优于 selected feature baseline；但纯回归 total normalized MAE 上，`svr_rbf_C10` feature baseline 为 0.878883，neural 为 1.004271，且 feature baseline 的 burial_depth MAE 更好。

可以接受：
- 可以接受。registry/manifest 显式加载成立；没有 latest/newest NPZ scan；模型输入只使用 `delta_b/BxByBz`；labels、shape、split、sample_id 只用于 supervision、metrics 和分组审计；train-only normalization、validation-only selection、test final only 成立；当前无 staged forbidden artifacts。

是否同意 21.2 只作为 training gate 而非 baseline：
- 同意。

是否同意下一步 A_expand_internal_defect_dataset：
- 同意。

简短原因：
- 21.2 证明 internal 分支存在可学习信号，但 val/test split 覆盖严重不完整：val/test 都只有 `internal_cuboid`，burial 覆盖也不全；N=96 不足以支撑稳健 shape/depth 泛化结论，所以应先扩展并重做分层数据，而不是升级 baseline 或继续小修模型。
