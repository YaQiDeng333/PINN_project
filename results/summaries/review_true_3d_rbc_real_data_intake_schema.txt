20.97 true 3D RBC real-data intake schema review

必须修复：无。
建议修复：sample table 的 format / axis_order / delta_b_unit 缺失检查，以及 data_file / sensor_x_m 模板占位 warning 已采纳。
可以接受：可以接受。
是否认为可以进入 real-data manifest dry run：可以。

原因：
- 未发现训练、COMSOL、data/NPZ/checkpoint/preview PNG 修改；CURRENT_BASELINE.md 无 diff。
- schema 覆盖 prepared delta_b 和 raw b_defect + b_no_defect 两种格式。
- sensor_z_m 是必填，单位 m，范围 [0.006,0.012]；缺失/越界是 blocker。
- no-defect reference、Bx/By/Bz、axis_order、三条 scan_line_y_m、sensor_x=201、Tesla 单位均写清。
- Bz-only 是当前路线 blocker；internal/buried defect 是独立 schema，不混入当前 surface RBC baseline。
- validator 支持 manifest-only validation，不要求真实 data 文件存在，并能阻止模板占位值被误判为 ready_for_inference。
- preprocessing plan 包含要求的 10 步，route decision 指向 real-data manifest dry run。
