已读取所有 9 个文件。以下为 15 项检查的逐项审查结果：

---

## 强制修复

无。

## 建议修复

1. **`scripts/sweep_comsol_rect_rot_neural_geometry_heads.py:366` — 评分公式注册？** `score_pred` 中使用 `-0.003 * stats["angle_mae_deg"]`，但 `stats["angle_mae_deg"]` 来自 `split_summary`，该函数会将非 rotated_rect 样本的 angle MAE 设为 NaN。需要确认 `split_summary` 是否正确忽略了 NaN（对 rect 样本 angle 为 NaN），否则 score 也会变成 NaN。但从结果来看所有得分均为有效浮点数，说明 base 模块已正确处理，仅建议在 future sweep 中显式文档化此行为。

2. **Type 过拟合严重** — train type_acc = 0.9925，val = 0.6515，test = 0.6515。差距 ~34pp，说明 type head 严重过拟合训练集。尽管输出符合 sweep 设计，但这是结果中的一个警示信号。

## 可以接受

1. ✅ **仅 delta_bz 作为输入** — `BzEncoder` 只处理信号，`forward(signal)` 无其他输入。
2. ✅ **defect_type / geometry_params / source_pack 未作输入** — 未传入模型。
3. ✅ **type / geometry labels 仅作 supervision** — 仅出现在 loss 计算中（`batch["type_target"]`、`batch["geom_target"]`）。
4. ✅ **可微 rasterizer 在 forward path 中** — `base.soft_rect_mask()` 用于 rect 和 rotated。
5. ✅ **无 true-type hard-route rasterizer** — 路由使用 softmix（type_prob 加权混合）或 unified（仅 rot）。
6. ✅ **5 个候选结构** — `shared_sincos_softmix`、`shared_boundedangle_softmix`、`shared_boundedangle_strongtype`、`shared_trunk_type_residual`、`unified_rotated_box_no_typemix`。
7. ✅ **candidate selection 仅使用 validation** — `select_best` 以 `val_score` 为主排序键。Test 仅在 selected candidate 上运行。
8. ✅ **Test 仅用于 final evaluation** — `run()` 第 679-683 行，test 仅对 selected model 评估。
9. ✅ **Train-only normalization** — `geom_mean`/`geom_std` 从 NPZ 加载（基于训练集的固定统计量）。
10. ✅ **Rasterizer validation 通过** — 摘要明确声明用真实几何体以 IoU=1.0000 验证通过。
11. ✅ **Polygon 合理排除** — 摘要："Scope: rectangular_notch + rotated_rect only; polygon parsed/reported but excluded."
12. ✅ **Metrics / summary 一致** — 交叉检查 CSV 与 summary 之间的多个值，包括 candidates.csv、geometry_summary.csv 中的 val/test IoU/Dice 与 summary 吻合。
13. ✅ **与 20.48 / 20.49 / Piao / dense baseline 正确对照** — 脚本第 53-66 行引用的值与 summary 输出均一致。
14. ✅ **Baseline docs 未修改** — `git status` 未显示任何已跟踪文件的修改。
15. ✅ **未提交 data / NPZ / checkpoint / preview PNG** — `git status` 仅显示 4 个未跟踪文件（sweep 脚本、临时审查文件、copy-output markdown、visualize_current_baseline.py），均非数据文件。

---

## Geometry head sweep 是否找到实质性改进？

**否。** 最佳候选 `shared_trunk_type_residual`（添加了 type-conditioned residual）相比 20.48 仅有边际差异：
- IoU: 0.5908 → 0.5877 (-0.0031)
- Dice: 0.7385 → 0.7351 (-0.0034)
- type_acc: 0.6364 → 0.6515 (+0.0151)
- angle_MAE: 20.14° → 20.73° (-0.59°，即更差)

所有 5 个候选的 val_score 高度聚集（1.0528–1.1056），表明架构变化未带来有意义的区分度。type accuracy 的 +1.5pp 提升被 segmentation 和 angle 的轻微下降抵消。

## 是否建议进入 forward consistency 下一步？

**否。** 接受门槛未通过（4 项中 3 项失败）：
- type_accuracy = 0.6515 < 0.72（未通过）
- angle_mae = 20.73° > 18.0°（未通过）
- IoU = 0.5877 < 0.6058 且 Dice = 0.7351 < 0.7505（未通过）
- area_error = 0.2678 ≤ 0.28（通过）

痛苦点依然是 type classification（23/66 test 样本类型错误）和 angle estimation（17/66 角度误差大）。继续沿用 delta_bz-only 架构进行 forward consistency 不太可能解决这些缺陷。摘要建议的 "B. Feature-assisted geometry head" 方向合理 — 在解决基础 type/angle 弱点之前，进一步差异化架构的边际收益看起来很小。

