20.93 independent read-only review: true 3D RBC liftoff trade-off strategy

Must-fix:
- None.

Suggested fixes:
- The first review noted that `true_3d_rbc_liftoff_tradeoff_failure_cases.csv` used `relative_change_pct` for a wMAE-specific row. The controller addressed this by adding explicit `profile_depth_rmse_relative_change_pct` and `wmae_relative_change_pct` fields and rerunning the outputs.

Acceptable:
- Yes.

Checks:
- No COMSOL run, no training run, and no data/NPZ mutation were found in this stage.
- The audit correctly cites 20.92 metrics: C0 non-nominal profile RMSE `0.000874310` and Dice `0.683351`; C1 non-nominal profile RMSE `0.000659761` and Dice `0.833129`; C1 nominal RMSE `0.000333059 -> 0.000809011`.
- The audit correctly treats C2 and C1 seed 42 as post-hoc diagnostics only; it does not test-reselect them.
- The C1 nominal degradation explanation is reasonable: unconditioned liftoff mixing, `sensor_z_m` unavailable to C1, nominal rows only one quarter of the pack, no nominal-preservation penalty, and held-out base-level generalization/forgetting.
- The recommended next route is reasonable: S3 baseline+liftoff adapter as primary, S2 revised sensor_z-conditioned model as secondary, and no continuation of unconditional C1 augmentation.
- `CURRENT_BASELINE.md` remains unchanged.
- Forbidden artifacts remain out of scope: data, NPZ, checkpoint, preview PNG, notes, baseline docs, and `scripts/visualize_current_baseline.py`.

Review decision:
- I agree with the C1 nominal degradation diagnosis.
- I agree with the S3/S2 recommendation.
- The stage is acceptable after the field-name clarification.
