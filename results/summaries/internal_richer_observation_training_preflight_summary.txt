# 23.1 internal richer-observation training preflight summary

created_at: 2026-05-31

## 范围

本轮执行 23.1 internal richer-observation training gate。前置要求是先读取 23.0 route decision，并且 23.0 必须明确推荐 R1 / R2 / R1+R2 范围内的输入配置。

## 当前状态

- 当前目录：`C:\Users\19166\Desktop\PINN_project`
- dataset_id：`comsol_internal_defect_richer_observation_pack_v1`
- registry：`COMSOL_DATA_REGISTRY.md` 中存在显式条目。
- manifest：`results/manifests/comsol_internal_defect_richer_observation_pack_v1.manifest.json` 存在。
- NPZ：由 manifest 指向，存在于 ignored `data/` 路径，不提交。
- 22.9 validation：通过，180 / 180 行，30 / 30 base paired completeness。
- 23.0 route decision：已补齐并通过。

## 23.0 训练前置结论

- route decision summary：`results/summaries/internal_richer_observation_evaluation_route_decision_summary.txt`
- decision matrix：`results/metrics/internal_richer_observation_evaluation_decision_matrix.csv`
- selected observation config：`R1_plus_R2_combined`
- selected variants：`R1_9line_z0p008`、`R2_5line_z0p006`、`R1_5line_z0p008`、`R2_5line_z0p010`、`R2_5line_z0p012`
- R3 / R4：本轮不允许进入训练。

## 安全边界

- 不运行 COMSOL。
- 不生成或修改 data / NPZ。
- 不更新 `CURRENT_BASELINE.md`。
- 不创建 baseline。
- 模型输入只允许 `delta_b/BxByBz`、`scan_line_mask`、`sensor_z_m` 和 delta_b-derived features。
- true `shape_type`、`burial_bin`、`size/aspect`、`split`、`sample_id` 只用于 supervision / metrics，不作为模型输入。

## 结论

preflight 通过，可以进入 23.1 loader/input validation、reference evaluation、training gate 和 route decision。
