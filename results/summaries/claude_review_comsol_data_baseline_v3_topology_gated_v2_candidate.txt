Now I have all the data needed. Here is the review.

---

**必须修复**

1. **cc2 未改善且回退于 v1**：v2 POC cc2 IoU=0.5592 vs 20.42 参考 0.5598（delta -0.0006），不满足 `cc2_improved` 门槛（IoU≥0.5700 或 Dice≥0.7120）。更关键的是 cc2 明显劣于 20.44 gated v1 POC（v1 cc2 IoU=0.5839, Dice=0.7300；v2 cc2 IoU=0.5592, Dice=0.7047），v2 在结构上引入 residual 却导致 cc2 相比 v1 实质性退步约 -0.025 IoU/-0.025 Dice。cc2 standalone baseline（MULTI_BASELINE: IoU 0.6118, Dice 0.7573）对比为 substantial degradation。

2. **Residual 机制几乎无效**：residual/base absolute ratio mean=0.0106，residual dominance rate=0.0。模型实际行为接近「base decoder + 极弱 residual 扰动」，核心设计意图（按任务拓扑动态适配）未生效。`LAMBDA_RESIDUAL=1e-4` 过弱，`RESIDUAL_SCALE=0.35` 无法驱动有意义的任务感知 residual 贡献。

3. **source_issue 标记为 True**：存在 source_pack 间 Dice 差异 >0.08 的情况，需排查数据源混杂是否影响 cc2 表现。

---

**建议修复**

1. 增大 `LAMBDA_RESIDUAL` 或 `RESIDUAL_SCALE` 以激活 residual 机制，否则 residual 分支只是训练开销无收益。
2. 对 cc2 弱样本单独进行 failure audit（已有 failure_cases CSV），确认 `rectangular_notch+rectangular_notch`（审计中最难组）是否是 cc2 退步主因。
3. 排查 v1→v2 的 cc2 退步是否与移除 v1 的完全按 gate 路由到独立 decoder、改为 base+residual 结构有关——v1 的「3 独立 decoder × softmax gate」对 cc2 效果更好。

---

**可以接受**

1. 模型输入设计：仅 delta_bz 输入，无 metadata 输入，gate 从 Bz latent 预测，task_group/component_count 仅作为辅助标签，无 true-label 硬路由——架构方向正确。
2. 训练协议：train-only normalization，validation-only checkpoint/threshold，balanced score 纯 validation，test final only——全部符合要求。
3. cc3 保持且改善：cc3 IoU=0.6887（+0.017 vs 20.42，+0.031 vs v1），cc3 Dice=0.8042（+0.016 vs 20.42，+0.032 vs v1），cc3_pred_cc_is_3=0.975（优于 20.42 的 0.9542）。
4. Single_defect 在 tolerance 内：IoU=0.6468 ≥ 0.6450 门槛。
5. Gate accuracy 97.3% 无 collapse，count head accuracy 97.3%。
6. 无数据文件/NPZ/checkpoint/preview PNG/notes 提交。
7. 未修改 baseline docs。

---

**是否认为 topology-aware residual gated decoder v2 值得继续：否**

cc2 未改善（POC 核心目标失败），且 cc2 相比更简单的 v1 gated 结构明显退步。residual 机制在当前超参下近乎无作用，增加的复杂度没有换回收益。不应继续此方向。

**是否可以进入 3-seed：否**

POC acceptance passed=False，`should_run_stage_b=False`，脚本自身已判定「stop and record rejection」。

**是否可以进入 documentation as candidate：否**

POC 未通过，不具备文档化条件。`recommended_next_step = "stop and record rejection"`。

---

**简短原因**

v2 相比 20.42 在 cc2 上无改善（IoU delta -0.0006，未达 ≥0.5700 门槛），相比 20.44 gated v1 在 cc2 上明显退步（-0.025 IoU/-0.025 Dice）。residual 机制在实际运行中接近零贡献（ratio=0.011），增加的复杂度未产生价值。cc3 改善不足以弥补 cc2 的核心短板。建议停止此方向，单独分析 v1 在 cc2 上为何有效（v1 的完全独立 branch decoder 结构对 cc2 友好），以此为线索设计下一版。
