所有关键文件已读取并相互验证。以下为复审结果。

---

## 必须修复

无。

## 建议修复

1. **COMSOL git status 的 `M` 前导空格不一致** — manifest 中 `comsol_git_status_short` 里 `" M src/tools/results.py"` 和 `" M src/tools/study.py"` 带有前导空格，而 `"M src/tools/physics.py"` 没有。这是 `git status --short` 的自然输出（staged vs unstaged），但作为 JSON 字符串不够干净，建议 trim 后再写入 manifest。

2. **NPZ SHA256 未由清单自身的生成逻辑计算** — 6 个文件中有 4 处引用了同一个 hash `28a13439…`，但没有说明这个 hash 的来源（是注入的还是在生成流程中计算的）。建议在 manifest 中注明 `npz_sha256_source: "script"` 或 `"manual"`，以便将来 audit。

## 可以接受

1. **库存完整覆盖 60 个样本** — CSV 包含 rbc_pilot_001 到 rbc_pilot_060，状态：30 pass + 2 fail + 12 not_attempted（LD_dominant 6 个 + WD_dominant 6 个 + boxy 深层变体 4 个）= 30 pass + 2 fail + 28 not_attempted。总计 60 行，与 `planned_rows: 60` 一致。未计入行数为 0。

2. **缺失曲率家族明确记录** — 所有 6 个文件一致声明 `LD_dominant` 和 `WD_dominant` 缺失。Route decision 声明 `train_ready: False`、`baseline_ready: False`、`next_step: top-up generation`。Registry 和 Manifest 中 `forbidden_use` 都包含 `automatic_mainline_training`、`baseline_update`、`current_baseline_replacement`。没有矛盾。

3. **Worktree / dirty 状态透明记录** — `COMSOL_DATA_REGISTRY.md` 包含了 `comsol_worktree_status_entries: 5`。Manifest 和 Registry 都记录了 `pinn_git_status_short` 和 `comsol_git_status_short`，与当前 `git status --short` 一致。提交 hash 被固定（`de9e108` / `928c82e`），没有被伪装成干净可复现提交。

4. **未训练、未建基线、未回退高层、未声称 exact Piao RBC** — `exact_piao_rbc: False`、`rbc_style_approximation: True`，每个 CSV 行在 `geometry_params.rbc_formula_status` 中都包含 `"exact Piao RBC formula not implemented"`。Summary 明确声明 "This is a pilot pack generation output, not a baseline" 和 "High-layer fallback is not used"。

5. **未提交 data / NPZ / checkpoint / PNG / .mph / CSV / temp STL** — `git status --short` 仅显示 5 个未跟踪文件：`COMSOL_DATA_REGISTRY.md` 及 4 个 `.py` 脚本。无任何数据文件被提交或暴露在未跟踪区。

---

## true 3D RBC pilot pack 是否可进入 training gate？

**否。** 当前只是 `partial_pilot_generated`，缺失 `LD_dominant` 和 `WD_dominant` 两个曲率家族（24 个未尝试样本中的核心缺失），`train_ready: False`，`baseline_ready: False`。必须完成 top-up generation 后才能进入 explicit pilot training gate。

## 下一步建议

运行 top-up generation，仅针对 24 个 `LD_dominant` + `WD_dominant` 的 not_attempted 样本，以及 2 个 `deep_elongated` 失败样本（rbc_pilot_012/024）重新尝试。完成后重新验证 → 若全部通过，则更新 `train_ready: True` 并进入 explicit pilot training gate。

## 简短原因

文档完整、一致、诚实。阻塞项不是文档问题，而是 26 个样本（24 not_attempted + 2 fail）尚未生成有效 forward 信号这一客观事实。文档正确地反映了这一状态。
