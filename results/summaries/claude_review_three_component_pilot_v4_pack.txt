Now I have the full picture. Here is the complete review.

---

## Pilot v4 Pack — Comprehensive Review

### 1. Final NPZ N=480 ✓
`delta_bz.shape = (480, 3, 201)`, NPZ = 4.5 MB. All 17 fields present with correct dtypes.

### 2. Split: 320 train / 80 val / 80 test ✓
Stratification policy (`seed=2026`): 24 buckets of 20 — `source × combo × distance`. Each bucket assigns 12 or 14 samples to train based on a cyclic `(source_idx + combo_idx) % 3` rule that ensures even coverage.

| Split | far | medium | near | pilot_v3 | v4_topup | Total |
|-------|-----|--------|------|----------|----------|-------|
| train | 108 | 106 | 106 | 160 | 160 | **320** |
| val   | 26  | 27  | 27  | 40  | 40  | **80**  |
| test  | 26  | 27  | 27  | 40  | 40  | **80**  |

### 3. component_count all 3 ✓
`{'3': 480}`. Inventory CSV confirms every row has `component_count=3`.

### 4. connected_component_count all 3 ✓
`{'3': 480}`. No mask merge, no fragments. Geometry-to-mask IoU verified in source packs.

### 5. 4 combinations × 120 ✓
```
rectangular_notch × 3 : 120
rect × 2 + rotated × 1 : 120
rect × 1 + rotated × 2 : 120
rotated_rect × 3        : 120
```
Per split: each combo gets 80 train / 20 val / 20 test.

### 6. Distance bins: near 160 / medium 160 / far 160 ✓
Numerical thresholds differ between sources (expected — different COMSOL spatial domains):
- pilot_v3: near=0.0129, medium=0.0168, far=0.0216
- v4_topup: near=0.0131, medium=0.0170, far=0.0218

Semantic bin labels preserved; model should predict continuous distances, not bins.

### 7. Source packs: pilot_v3 (240) + pilot_v4_topup (240) ✓
Sources verified: both NPZ files exist, pass validation, coordinates match. `source_pack` field exported into final NPZ for provenance tracking.

### 8. Train/val/test cover all combination × distance × source ✓
Each split explicitly covers all 4 combos, all 3 distance bins, both sources. No structural blind spots.

### 9. delta_bz = bz_defect − bz_no_defect ✓
Passed `np.allclose(rtol=1e-7, atol=1e-12)` in source packs and final pack. All finite. Per-scan-line variation exists (not duplicated lines).

### 10. components_json all parseable ✓
All 480 entries parse as valid 3-component JSON. `geometry_key` uniqueness confirmed (zero duplicates). Sample ID uniqueness confirmed.

### 11. Deliverables generated

| Deliverable | Status | Path |
|---|---|---|
| NPZ | **Yes** | `data/comsol_mfl/prepared/...pack_v4_pilot.npz` (4.5 MB) |
| Inventory CSV | **Yes** | `results/metrics/...pilot_v4_inventory.csv` (480 rows) |
| Summary TXT | **Yes** | `results/summaries/...pilot_v4_pack_summary.txt` |
| Training gate script | **No** | Missing — `train_comsol_three_component_multi_defect_pilot_v4_gate.py` |
| Checkpoint | **No** | Requires training gate first |
| Preview PNG | **No** | Generated by training gate, not pack prep |

### 12. PINN Training Gate Readiness

| Criterion | Status |
|---|---|
| Schema (17 fields, all correct) | ✓ |
| No NaN/Inf in signals | ✓ |
| delta consistency | ✓ |
| Masks valid (3-CC, non-empty) | ✓ |
| Sample IDs unique, no geometry dup | ✓ |
| Coordinates monotonic | ✓ |
| Split stratification complete | ✓ |
| Source pack provenance tracked | ✓ |
| **Training gate script** | **✗ BLOCKING** |

**Adaptation needed for v4 gate script** (based on `train_comsol_three_component_multi_defect_pilot_v3_gate.py`):
1. `DEFAULT_NPZ` → v4 pack path
2. `EXPECTED_SPLITS` → `{"train": 320, "val": 80, "test": 80}`
3. Shape check in `validate_npz` → add `(480, 3, 201)` and `(320, 3, 201)`
4. `expected_split_combo_counts` → add branch for N=480: `(("train", 80), ("val", 20), ("test", 20))`
5. All output paths → `v4_*` variants
6. Handle new `source_pack` field (present in v4 NPZ but not v3)
7. Expected combo counts per split need to reflect 480-sample math
8. New expansion plan path: `comsol_three_component_multi_defect_pilot_v5_expansion_plan.txt`

**Risks to monitor:**
- **Distance bin semantic drift**: v3 and topup use different numerical thresholds for the same labels — train normalizer could learn source-specific artifacts
- **No area/depth stratification**: current pack only stratifies on combination and distance, not component area or depth ratios (noted in expansion plan as a goal)
- **Source pack signal leakage**: since source indicates which COMSOL batch, the model could learn to identify source rather than defect geometry if not properly regularized

**Verdict:** Pack preparation is clean. Data integrity checks pass. The single blocking gap is the missing v4 training gate script. Once created, the pack is structurally ready for PINN training.
