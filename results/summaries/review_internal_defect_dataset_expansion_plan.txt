21.3 internal defect dataset expansion plan 独立只读 review

必须修复：
- 无。

建议修复：
- 无硬性建议。PowerShell `Get-Content` 仍可能把中文显示成乱码，但用 Python 按 UTF-8 读取脚本和关键 summary/CSV，实际内容是正常中文，不是文件损坏。

可以接受：
- 可以接受。本轮仍是 plan-only；未 staged 文件；未发现 COMSOL、training、data/NPZ 操作；`CURRENT_BASELINE.md` 无 diff。
- manifest/registry 通过 `comsol_internal_defect_pilot_pack_v1` 显式加载。
- coverage audit 正确指出当前 N=96 的 blocker：val/test 只有 `internal_cuboid`，test 只覆盖 `deep/deep_plus`。
- expansion plan 满足 N=240、source=96、selected top-up=144、planned top-up=168。
- `expected_coverage.csv` 已新增 18 行 `internal_ellipsoid/internal_cuboid × aspect × split` 的明确 coverage gate，全部通过。

是否同意进入 21.3b pack generation：
- 同意。

简短原因：
- split blocker 和扩展目标已经锁定；下一步应按 21.3b 生成 top-up COMSOL pack，再 assemble/validate v2_240，21.4 才训练。
