I now have all the evidence needed. Here is the final review:

---

## 最终复审：20.70 imported watertight solid solver robustness diagnostic

### 必须修复

**无。**

---

### 建议修复

1. **`true_3d_imported_solid_solver_robustness_probes.csv` 仍未重新生成** — probe #2 (`material_domain_fixed`) 的 `domain_material_audit_pass` 仍为 `False`，而代码 `test_true_3d_imported_solid_solver_robustness.py:318-319` 已在 `material_fix_applied=True` 且 solve 成功时将其设为 `True`。CSV 与代码不一致。**但不阻塞管线**：NPZ 中 `domain_material_audit_pass=True`（以 schema validation 通过为证），route decision 基于 NPZ 而非该 CSV。

2. **`scripts/validate_true_3d_imported_watertight_forward_smoke.py` 有未提交的 diff** — 从 20.69 升级到 20.70 的变更（新增 solver metadata 字段、route decision 写入）尚未提交。建议提交以固化 20.70 状态。

---

### 可以接受

| # | 检查项 | 状态 | 证据 |
|---|--------|------|------|
| 1 | UTF-8 可读 | ✅ | `file` 输出 `UTF-8 text, with CRLF line terminators`；Read 工具正常解析全部 39 行 |
| 2 | 无训练模型 | ✅ | validate 脚本明确声明 "No training, surrogate, refinement, or baseline update" |
| 3 | 无创建 baseline | ✅ | git log 无 baseline 变更；review 已确认 |
| 4 | 无修复 mesh / Boolean | ✅ | preflight scope 明确禁止；probe #1 在原始几何上复现了 cavity 空选择失败 |
| 5 | domain audit 动态记录 | ✅ | audit CSV 显示 `dynamic_selection_used=True`, `domain_id_hardcoded=False`；steel/air/cavity 动态查询 |
| 6 | material fix 最小修复 | ✅ | probe #2 仅 `material_fix_applied=True`（external_air - steel + cavity），几何不变，无 high-layer fallback |
| 7 | solver probes bounded | ✅ | 12 个探针，mesh ∈ {4,5,6}，solver ∈ {default, direct}，scale ∈ {0.0,0.1,0.3,1.0}，含 ramp |
| 8 | Jscale=1.0 门控 | ✅ | `abs(full_source_jscale - 1.0) <= 1.0e-12`；<1.0 仅诊断用 |
| 9 | selected_solver_protocol 已记录 | ✅ | `"default"` 出现在 NPZ、route summary、CSV |
| 10 | Bx/By/Bz 真实导出 | ✅ | norm = 0.0153, 0.0023, 0.0136（有限非零） |
| 11 | delta_b 验证 | ✅ | `delta_max_abs_error = 0.0`（`defect - no_defect` 与 NPZ delta_b 完全一致） |
| 12 | NPZ/schema 验证 | ✅ | 26 个必填字段，形状 (3,3,201)，轴名 ["Bx","By","Bz"]，有限值，深度一致性，材料/求解器门控 |
| 13 | route decision | ✅ | `A_imported_solid_solve_forward_validation_pass` → smooth/mesh-based pilot；`high_layer_fallback=False` |
| 14 | 无修改 baseline docs | ✅ | git status 仅 `validate_*smoke.py` modified + `visualize_current_baseline.py` untracked（排除） |
| 15 | 无提交 data/NPZ/checkpoint/PNG/.mph/CSV/STL | ✅ | 近 5 次 commit 无禁止文件；`git diff` 范围限于 validate 脚本 |

---

### 是否认为 imported watertight solid solve/forward 已可用

**是。** 实际管线（domain audit → bounded probes → material fix → default solver Jscale=1.0 solve → forward smoke → NPZ → schema validation → route decision）完整自洽。NPZ schema validation 全部通过。上一轮唯一的阻断项（UTF-16 编码）已修复。

### 是否建议进入 smooth/mesh-based true 3D pilot generation

**是。** Route decision 正确：default solver + mesh_auto_size=5 + 最小 material fix 在 Jscale=1.0 下全源通过。未使用 direct solver，无可扩展性风险。high-layer fallback 被正确拒绝。

### 简短原因

UTF-16 编码已修复为 UTF-8。管线（domain audit → solve → forward → NPZ → schema validation → route decision）全门控通过。唯一残留问题是 COMSOL robustness probes CSV 未与代码同步重新生成，但 NPZ 门控正确，不阻塞进入 pilot。
