21.6 internal defect burial-depth refinement 只读 review

必须修复：
无。

建议修复：
1. 部分历史 summary / 脚本注释存在中文显示乱码，建议后续集中整理为 UTF-8 可读中文。
2. candidate screen 中非 selected candidate 也记录了 test 指标；已在 summary 中补充说明：这些 test 指标只作诊断，不参与候选选择。

可以接受：
可以接受。21.6 通过 COMSOL_DATA_REGISTRY.md 和 comsol_internal_defect_pilot_pack_v2_240 manifest 显式加载 dataset_id，未发现 latest/newest scan、COMSOL 调用、NPZ 写入、checkpoint 保存、CURRENT_BASELINE 修改或 forbidden staged artifacts。B2 只使用 delta_b-derived features；B3 使用 predicted shape logits，没有 true shape 输入泄漏。

B2 feature-fusion burial head 是否有实质价值：
有实质价值。B2 refined test burial_depth MAE = 0.413 mm，优于 21.4 neural 0.595 mm，也优于 feature baseline 0.472 mm；total normalized MAE 也从 0.406366 改善到 0.395256。

是否同意不更新 CURRENT_BASELINE：
同意。internal defect 仍是独立分支，本轮是 refinement candidate，不是 baseline transition。

下一步建议：
进入 internal benchmark rerun / candidate upgrade，但保留 burial-depth feature-fusion 风险审计。

简短原因：
B2 的收益来自合法的 delta_b-derived features，selection 是 validation-only，multi-seed 只跑 selected candidate；主要剩余风险是文档编码和 test 指标展示口径，不影响核心实验结论。
