21.4 internal defect v2_240 training gate review

必须修复：无。

建议修复：提交前只用白名单显式 staging；当前没有 staged 文件，results/ 下指标和 summary 若需提交通常需要 git add -f 指定路径。

可以接受：可以接受。

是否同意进入下一步：同意。21.4 满足 training gate 口径，但不是 baseline；CURRENT_BASELINE.md 未改，internal defect 仍是独立分支。

下一步建议：进入 E_prepare_internal_benchmark_report，整理 internal defect v2_240 formal benchmark/report；不要直接升 baseline。

简短原因：数据通过 COMSOL_DATA_REGISTRY.md + manifest 显式加载 comsol_internal_defect_pilot_pack_v2_240，无 latest/newest scan；输入链路只使用 delta_b -> (N,9,201)，labels/shape/split/sample_id 只用于 supervision 和 metrics；归一化来自 train split；epoch/seed/model 选择走 validation，test 只 final；未发现 COMSOL、data/NPZ 修改、checkpoint 保存、baseline 更新或 forbidden artifacts staged。
