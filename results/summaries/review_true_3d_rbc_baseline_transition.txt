- 必须修复：无。

- 建议修复：
  1. `CURRENT_BASELINE.md` 的旧 2D 段落下方仍保留了历史标题“当前权威 boundary baseline”等原文，虽然外层已标成 archived comparator，但提交前建议加一句“以下旧段落为历史原文，当前权威以 20.86 顶部段落为准”，降低误读风险。
  2. 当前 `git diff --cached --name-only` 为空，后续提交时需要白名单 staging；`results/` 被 ignore，若提交 report metrics/summaries 只能对允许文件逐个 `git add -f results/...`，不要用 `git add .`。

- 可以接受：可以接受。检查到当前未 stage 任何文件，modified/untracked 范围是 `CURRENT_BASELINE.md`、`EXPERIMENT_LOG.md`、`NEXT_STEP.md`、`PINN优化路线.md`、`scripts/summarize_true_3d_rbc_benchmark_report.py`，未发现 data/、NPZ、checkpoint、preview PNG、notes、`__pycache__` 准备提交风险。report script 只读取 registry/manifest/formal metrics 并生成 summary/CSV，不训练、不运行 COMSOL、不读写 NPZ。

- 是否同意 baseline transition：同意。`CURRENT_BASELINE.md` 已明确从旧 2D v3_complex mask/boundary baseline transition 到 true 3D RBC profile-depth baseline；20.77/20.85 指标准确：seed=42，train/val/test `0.646111/0.748694/0.678014`，profile RMSE `0.000387737 m`，Er-like `0.340544`，L/W/D `1.892/2.186/0.800 mm`，wMAE `0.201076` auxiliary，IoU/Dice `0.750650/0.847727`。

- 是否同意旧 2D baseline 仅作 comparator：同意。旧 2D baseline 未删除，已放在 archived comparator role；20.81 被正确定位为 visual/mask comparator，20.83 被正确定位为 profile-primary negative gate。

- 下一步更建议什么：准备 benchmark/report 展示，同时优先做 real-data alignment；不要继续把 20.81/20.83 升级为 baseline。

- 简短原因：20.86 的文档和 report package 把 baseline transition、dataset identity、method chain、主/辅指标、comparator roles、limitations 都写清楚了；也明确 `exact_piao_rbc=false`、RBC-style / Piao-inspired approximation、未真实实验验证、非 deployment-ready。

主控处理：已采纳建议 1，在 `CURRENT_BASELINE.md` archived comparator 段前补充“以下旧段落为历史原文，当前权威 baseline 以第 20.86 顶部段落为准”。建议 2 将在提交前通过白名单 staging 和 cached diff 检查执行。
