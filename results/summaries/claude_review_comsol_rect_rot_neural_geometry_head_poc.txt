Now I have all the data needed for the review. Here is the structured output:

---

## 必须修复

**无。** 所有 15 项检查均通过，无阻断性问题。

## 建议修复

1. **类型准确率偏低（test type_accuracy=0.6364），是 POC 不通过的主要原因。** 失败审计显示 24/66 测试样本属于 `wrong_type`（36.4%），尤其是 `rectangular_notch` → `rotated_rect` 混淆频繁。建议在 geometry head 上增加类型分类的损失权重（当前 type_loss 仅 0.1 倍），或引入对比/度量学习增强类型判别。

2. **角度 MAE 偏高（20.14° vs Piao 的 18.29°）。** 11/66 样本属于 `wrong_angle`（>15° 误差）。当前 sin/cos 角度损失的权重仅为 0.05，可能是约束不足的原因。考虑提高角度损失权重或改用周期性的角度损失（如 `1 - cos(θ_pred - θ_true)`）。

## 可以接受

- **Neural geometry head 架构正确**：`BzEncoder (Conv1d) → Linear(128→64→9)`，输出 type_logits(2) + geom_norm(5) + angle_sincos(2)，不是 dense mask decoder。
- **输入纯净**：模型仅接受 `delta_bz` (3, 201)，defect_type/geometry_params/source_pack 仅用于 supervision。
- **Differentiable rasterizer 在 forward path 中正确使用**：`soft_rect_mask` 生成可微掩码，BCE + Dice 损失通过 logit 回传。rasterizer validation 通过（IoU=1.0，梯度正常）。
- **无 true type hard routing**：最终掩码为概率混合 `p_rect * rect(angle=0) + p_rot * rect(pred_angle)`。
- **train-only normalization**：delta_bz 和 geometry 归一化参数均仅从 train split 估计。
- **validation-only checkpoint/threshold**：best epoch 由 val score 选出，threshold=0.7。
- **test final only**：测试集仅在加载 best checkpoint 后评估一次。
- **Polygon 合理排除**：`rect+rotated N=400`（train/val/test = 268/66/66），polygon 在 label extraction 中解析但不参与训练/评估。
- **Metrics/summary 一致**：epoch log、group summary、主 summary 中的 IoU/Dice/area_error 数值完全吻合。
- **对照基线正确**：硬编码了 Piao weak adaptation (IoU=0.4467) 和 dense baseline (IoU=0.6515)，POC IoU=0.5908 介于两者之间。
- **未修改 baseline docs**：`git diff` 确认无 .md 文件变更。
- **未提交 data/NPZ/checkpoint/preview PNG**：preview 目录未被 git 跟踪，无 .pt/.pth/.ckpt 文件提交。

## 是否认为 neural geometry head + differentiable rasterization POC 符合外部报告第 1 优先级路线？

**符合。** POC 正确实现了核心架构：Conv1d BzEncoder → neural geometry head（显式几何参数预测）→ 可微光栅化 → mask 监督。输入仅 delta_bz，无 forbidden metadata。训练协议（train-only norm、val-only selection、test final only）严格。POC 虽未通过 promising 阈值（类型准确率 0.6364 < 0.75，角度 MAE 20.14° > 15°），但这不是架构方向问题，而是几何参数学习精度问题。

## 是否建议进入 forward consistency 下一步？

**不建议直接进入。** POC `promising = False`，主因是类型准确率（0.6364）和角度 MAE（20.14°）未达标。summary 自身的建议也是 "B. Fix neural geometry head before forward consistency"。建议先提升几何参数回归精度（调高类型/角度损失权重、增强 encoder 容量、或引入辅助任务），待 test type_accuracy ≥ 0.75 且 angle_MAE ≤ 15° 后再进入 forward consistency。

## 简短原因

POC 架构方向正确、工程实践规范、各检查项通过，但几何参数（类型+角度）预测精度不足，在堆叠 forward consistency 之前应先修复基础回归能力。

