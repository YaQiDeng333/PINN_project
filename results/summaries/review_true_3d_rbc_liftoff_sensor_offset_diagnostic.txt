20.90 true 3D RBC liftoff / sensor-offset diagnostic review

Review agent: Chandrasekhar
Review mode: independent read-only review
Must-fix loop: 1

必须修复:
- none. The original must-fix was resolved. nominal_replay now compares the 12 regenerated COMSOL nominal raw rows against the 20.88a/20.85 clean prediction artifact for the same base_sample_id rows, not against itself.

建议修复:
- Low priority: true_3d_rbc_liftoff_sensor_offset_plan.csv contains full profile/mask JSON payload. This is acceptable for the current diagnostic manifest, but a future plan format could be slimmer and join labels through registry/manifest.

可以接受:
- yes.
- No training was found.
- CURRENT_BASELINE.md has no diff.
- No staged forbidden artifacts were found during review.
- Raw and calibrated evaluations are both reported.
- Plan has 132 evaluation rows: 96 COMSOL rows and 36 postprocess rows.
- COMSOL inventory records 96/96 successful rows.
- Evaluation metrics contain 264 rows: 132 raw + 132 calibrated.

是否同意最敏感因素判断:
- yes. liftoff is the most sensitive factor. liftoff_z_0p012 raw profile degradation is 627.747%, and liftoff group raw mean degradation is 334.947%.

是否同意 calibration caveat:
- yes. Source/amplitude raw input fails but calibrated input is green; liftoff remains failed after calibration. Calibration is therefore an acquisition diagnostic caveat, not a baseline replacement.

下一步建议:
- Enter dedicated liftoff COMSOL robustness / augmentation data design before internal defect feasibility.

简短原因:
- The nominal replay gate now uses a real clean-artifact comparison. Source/Jscale sensitivity can be mitigated by the fixed 20.89 calibration protocol, but liftoff causes profile-depth failure that calibration does not solve.
