All checks are complete. Here is the final review.

---

## Stage 20.36 COMSOL Combined Baseline V2 Candidate — Review

### 1. 准备脚本 `prepare_comsol_combined_baseline_v2_pack.py`

**合并正确性** ✓
- `single_pilot_v9` (600样本) + `multi_defect_pilot_v3` (240样本) → 840样本
- 坐标数组 (`sensor_x`, `scan_line_y`, `mask_x`, `mask_y`) 逐元素比对一致性 (line 442)
- `delta_bz` = `bz_defect - bz_no_defect` 全局验证 (line 134)

**Schema 一致性** ✓
- 输出包含 21 个必需字段，readback 验证 schema 完整无缺失
- `component_counts`: 单缺陷=1 (ones)，多缺陷=2 (从源 NPZ 直接复制)
- `connected_component_counts`: 单缺陷从 mask DFS 计算，多缺陷从源 NPZ 保留
- `geometry_params` / `components_json` / `component_types` 均可解析

**样本去重** ✓
- 单缺陷 `sample_id` 前缀 `single_`，多缺陷前缀 `multi_`，无冲突
- Line 452: `len(set(sample_ids)) != len(sample_ids)` 会抛异常

**分割保持** ✓
- 源 split 标签原样保留，不做任何 reshuffle 或重平衡
- 最终分布: train=562, val=139, test=139

**坐标一致性** ✓
- 四个坐标数组在两源 NPZ 间完全匹配 (line 442-444)

---

### 2. 训练脚本 `train_comsol_combined_baseline_v2_candidate.py`

**输入仅 delta_bz** ✓
- `normalized = (delta_bz - train_mean) / train_std`，仅使用 delta_bz (line 476)
- `ComsolSmokeDataset` 输入为 normalized delta_bz + masks，模型 forward 只接受 signal

**无元数据泄露** ✓
- 训练循环仅使用信号和目标 mask；`defect_type`/`defect_group`/`geometry_params` 不在训练路径中

**仅训练集归一化** ✓
- Line 474-475: `train_mean` / `train_std` 仅从 `splits["train"]` 计算
- 同一归一化参数应用于 val/test/all

**仅验证集选择检查点/阈值** ✓
- Line 530: `best_state` 基于 `best_val_score` 选择
- Line 532: `selected` 阈值在 val set 上通过 `select_threshold_for_epoch` 选择
- 测试集仅在最终评估（line 533）使用，不参与训练或选择

**无 v3_complex 使用** ✓
- 搜索仅命中 Line 778 的注释说明"not v3_complex"，非实际使用

**无 CURRENT_BASELINE 修改** ✓
- 同样仅出现在注释中

---

### 3. 结果

**Seed 摘要一致性** ✓
- 3 seeds × 3 splits = 9 行，每 seed 内 train/val/test 的 `sample_count` 一致（562/139/139）
- 所有 seed 的 `train_loss_decreased` = True，loss 正常收敛
- `best_epoch` 在 16-29 之间，合理

**分组摘要一致性** ✓
- `defect_group`: single_defect (600) + multi_defect (240) = 840 ✓
- `defect_type`: rectangular_notch (200) + rotated_rect (200) + polygon (200) + multi_defect (240) = 840 ✓
- `component_type_combination`: 7 种组合 + polygon_component_present (2 组)，每组样本数正确

**连通分量指标正确性** ✓
- 所有 single_defect → pred_cc=1, true_cc=1
- 所有 multi_defect → pred_cc=2, true_cc=2
- `predicted_component_count_correct_rate` = 1.0（train 上有一个零星的 split 错误，seed 123: 0.9982，属于正常波动）
- 无 missed / merged 组件

**Failure Audit 合理性** ✓
- 最差样本集中在 multi_defect 和 rotated_rect/polygon 类型
- 主导失败模式：`area_error`（面积偏差 > 35%）
- 次常见模式：`localization`（中心误差 > 8px），主要出现在多边形组件上
- 最难分组：`rectangular_notch+rectangular_notch` (dice=0.7484)，合理

**联合训练是否损害单/多任务** ✓
- Single test: combined IoU=**0.6601** vs baseline 0.6515 → **提升 1.3%**，未退化
- Multi test: combined IoU=**0.6088** vs baseline 0.6118 → **下降 0.49%**，在 3% 阈值内，未退化
- Multi pred_cc_is_2: **1.0**，完美保留组件识别能力

---

### 4. 文件安全

**无数据/NPZ/检查点/预览PNG/notes 提交** ✓
- `bc8318c` (prepare): inventory.csv + pack_summary.txt + 脚本，无 NPZ
- `b794b09` (training): CSV + TXT 结果文件 + 脚本，无 PNG/checkpoint/NPZ
- `git ls-tree` 确认仓库中无 `.npz`/`.png`/`.pt` 文件

---

### 最终判定

| 类别 | 结论 |
|------|------|
| **必须修复** | 无 |
| **建议修复** | 1. `prepare` line 86: 变量名 `labels` 改为 `visited`（语义更准确）<br>2. `prepare` line 150: `depth` 默认 `0.0` 使 `depth is not None` 恒为 True，None 路径为死代码 |
| **可以接受** | 是 |
| **可以建立 COMSOL_DATA_BASELINE_V2** | **是** |
| **简短原因** | 联合训练未损害单缺陷或多缺陷任务（单 IoU +1.3%，多 IoU -0.49%），多缺陷组件计数完美保持（pred_cc_is_2=1.0），无数据泄露，split/coords/schema 均一致，训练流程规范（train-only normalization + val-only checkpoint selection + test final only），文件安全（无二进制文件提交）。 |
