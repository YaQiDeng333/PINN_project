22.6 internal defect tail-risk gate 只读 review

结论：PASS，未发现 must-fix。

检查项：
- py_compile：4 个 22.6 脚本均通过。
- forbidden artifacts：未发现 data/、NPZ、checkpoints/、results/previews/、notes/、CURRENT_BASELINE.md、scripts/visualize_current_baseline.py 的 staged 或 dirty 风险。
- label leakage：risk gate 的训练矩阵只使用 RISK_FEATURE_COLUMNS；sample_id、split、true_shape_type、burial_depth_level、size_level、aspect_bin 仅用于 metadata、target 或分组审计，不进入模型输入。
- train-only normalization：train_scale(x, train) 只用 train mask；模型 fit 只使用 train split。
- validation-only selection：模型和 threshold 选择只扫描 validation scores；test 只在 selected threshold 固定后做最终评估。
- contract：已导出 f2_pred_train_range_violation_count 所需的 7 维 train label low/high 常量。
- 结果一致性：selected=random_forest_small，threshold=0.07046389；test catastrophic recall=1.000，geometry recall=1.000，false alarm=0.417，coverage=0.283；route decision 指向 A_internal_inference_smoke_with_abstention。

口径确认：internal branch 仍不是 baseline；risk gate 只能作为 abstention / high-risk 标记，不把高风险样本写成稳定 center/burial 推理。
