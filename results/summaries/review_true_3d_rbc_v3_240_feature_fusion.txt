第 20.81 feature-fusion neural model review

必须修复：
无。

建议修复：
已处理。review agent 建议将 audit/decision 中的 “Did fusion improve curvature?” 改为 “Did fusion reach >=0.01 substantive curvature improvement?”，避免 raw curvature 小幅下降与本轮实质改善门槛混淆。已更新 scripts/audit_true_3d_rbc_v3_240_feature_fusion_results.py 并重跑 audit。

可以接受：
可以接受。dataset_id 通过 COMSOL_DATA_REGISTRY.md + manifest 显式加载；未使用 latest/newest NPZ 自动扫描；未运行 COMSOL；未修改或生成数据/NPZ；未创建 baseline；未修改 CURRENT_BASELINE.md 或 COMSOL baseline docs。feature matrix 仅使用 20.80 delta_b/BxByBz 派生的 F0__..F5__ 特征列，sample_id/split/template/depth_bin/aspect_bin/projected_mask/rbc_params 没有作为模型输入。feature imputer/scaler、BxByBz normalization 和 target scaling 均使用 train-only 统计；candidate screen 使用 validation-only selection；multi-seed 使用 validation-only seed selection；test 只用于 validation-selected candidate/seed 的 final evaluation。

是否认为 feature-fusion 对 curvature 有实质价值：
不认为。selected multi-seed result 为 H3_curv_fusion_F0F1F2_w1p0 seed=2026，test curvature MAE 从 20.77 neural 的 0.201076 降到 0.194483，但改善仅 0.006592，未达到本轮 0.01 实质改善门槛，也弱于 20.80 feature-only 的 0.190304。wLD 从 0.209439 退化到 0.217079。

是否建议 formal benchmark rerun / targeted data / label redefinition：
不建议 formal benchmark rerun。下一步更建议 redefine curvature labels/output representation；curvature-targeted data 可作为后续，但当前证据更像是 curvature 输出定义/可辨识性问题，而不是简单 feature-fusion 可解决的问题。

简短原因：
20.81 改善了 total MAE 和 projected mask Dice：total 0.678014 -> 0.667888，Dice 0.847727 -> 0.866573。但 curvature 没达到实质改善门槛，wLD 退化，L/D 与 depth RMSE 存在 trade-off。因此本轮结论应是 feature-fusion 改善整体拟合和 mask，但不足以解决 curvature 风险。
