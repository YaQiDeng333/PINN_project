23.2 internal multi-scan-direction plan review

结论：review 通过，无 must-fix。

审查范围：只读检查 23.2 三个新增 plan-only 脚本、生成的 summaries/metrics、当前 git 状态和禁止提交项。未发现 COMSOL 调用、训练调用、data/NPZ/checkpoint/preview/notes 写入逻辑，也未发现 `CURRENT_BASELINE.md` 修改。

已确认事项：
- 23.2 是 plan-only：无 COMSOL、无训练、无 data/NPZ mutation、无 baseline 更新。
- 23.1 失败证据引用清楚：`R1_plus_R2_combined` / `O3_richer_observation_tail_aware` 未形成 stable inference candidate，shape F1 `0.600000`，catastrophic `4/5`，geometry branch `1/5`。
- COMSOL 机制判断正确：现有 richer-observation generator 有 `scan_direction` metadata，但 sensor points 仍复用 fixed x-path builder；23.2b 必须新增真正的 direction-aware sensor point builder。
- 候选矩阵包含 D0-D4，D1/D2 作为 22.9 后的第一轮默认方向化诊断，D3 multi-liftoff + dual-direction 和 D4 multi-magnetization 暂缓。
- diagnostic pack plan 复用 22.9 的 30 个 base，只计划补生成 `D1_y_scan_5line_z0p008` 和 `D2_y_scan_9line_z0p008`，planned rows `60`，fallback rows `48`。
- 30 个 base 均有 D1/D2 paired y_scan rows，可与既有 `R1_5line_z0p008` / `R1_9line_z0p008` x_scan 配对。
- 未来数据契约写清：assembled tensor `delta_b=(N,3,2,9,201)`，包含 `direction_mask`、`scan_line_mask`、`direction_names`、path/line coordinate axis 与坐标范围。

备注：preflight 中的 forbidden artifact 状态已按 review 建议改成 `forbidden_artifact_status_nonignored_tracked_paths`，明确它只代表非 ignored tracked/untracked 路径检查干净，不声称历史 ignored data/checkpoint/preview 目录为空。

staged whitelist 结论：本轮允许提交 scripts、results/summaries、results/metrics 和三份 Markdown；禁止 data、NPZ、checkpoint、preview PNG、notes、`.mph`、raw CSV、temp STL、`CURRENT_BASELINE.md`、baseline docs 和 `scripts/visualize_current_baseline.py`。
